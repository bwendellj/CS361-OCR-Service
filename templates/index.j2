<!DOCTYPE html>
<html lang="en">
<head>
    <title>Upload Image for OCR</title>
</head>
<body>
    {% if msg %}
    <h1 style="color:green"> {{ msg }} </h1>
    {% endif %}

    <br>
    <h1> Upload Image File - jpg, jpeg, or png are accepted filetypes</h1>

    <form method=POST enctype=multipart/form-data>
        <input type="file" name=file>
        <input type="submit" name=Upload value="Upload">
    </form>

    {% if extracted %}
        <h1> Results: </h1>
    {% endif %}

    {% if img_src %}
        <img src="{{ img_src }}">
        {{ img_src }}
    {% endif %}

    {% if extracted %}
        <p> {{ extracted }} </p>
    {% endif %}

</body>
</html>